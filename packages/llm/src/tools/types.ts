/**
 * External Tools Type Definitions
 *
 * Types for MCP tools (Model Context Protocol).
 * For unified tool types, use ./unified/types instead.
 *
 * @module tools/types
 */

// ============================================================================
// MCP TYPES (re-exported from @journey/mcp)
// ============================================================================

export type {
  MCPTransport,
  MCPStdioServerConfig,
  MCPHttpServerConfig,
  MCPServerConfig,
  MCPServersConfig,
  MCPClientOptions,
  MCPTool,
  MCPToolCallRequest,
  MCPToolCallResponse,
  MCPHealthStatus,
  MCPServiceClientOptions,
} from "@journey/mcp";
