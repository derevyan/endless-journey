/**
 * Common shared values and types
 *
 * Centralized constants and types used across multiple schema modules.
 * This prevents duplication and drift between related schemas.
 *
 * @module schemas/common
 */

export * from "./status";
