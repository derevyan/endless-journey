/**
 * Uploads Module
 *
 * Public API for the uploads domain.
 * Handles file uploads for media attachments (images, videos, avatars).
 *
 * @module modules/uploads
 */

export { uploads } from "./routes";
